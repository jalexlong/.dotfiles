# Lightweight idle behavior for Chromebook

exec swayidle -w \
    timeout 300 'swaylock -f -c 11111b' \
    timeout 420 'swaymsg "output * power off"' \
    resume 'swaymsg "output * power on"' \
    before-sleep 'swaylock -f -c 11111b'
